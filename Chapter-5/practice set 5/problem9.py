# Can you change the values inside a list which is contained in set S? 
# s = {8, 7, 12, "Harry", [1,2]} 

s = {8, 7, 12, "Harry", [1,2]}

s[4][0]=8    #  First of all we can't include list([1,2]) in set. And set are immutable so they can't change